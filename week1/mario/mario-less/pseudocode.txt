prompt the user for the height of the pyramid
if the height is less than 1 or greater than 8 or not an int at all, the program will prompt again the user for the height
iterate from 1 through height:
    1. On iteration x, print x hashes and then a newline